import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  ExpandContainer: {
    width: "100%",
    height: 150,
    top: -20,
    zIndex: -1,
    borderRadius: 10,
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "space-around"
  },
  icon: {
    width: 50,
    height: 50
  }
});
